# NetChess3D

A 3D networked chess game, built on a custom C++ / DirectX 11 engine.

## How to Use

Open `NetChess3D.sln` in Visual Studio 2022, then build (`Ctrl + Shift + B`). The executable is in the `Run/` folder.

## Status

In development.
